# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
*.swp

# Despliegue / build (GitHub Pages sirve directo desde la raíz)
_dist/
_build/

# Netlify (los .env no se suben; netlify.toml SÍ se versiona)
.env
.netlify/